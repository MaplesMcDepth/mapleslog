---
import { Image } from 'astro:assets';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import Header from '../components/Header.astro';

interface Props {
  title: string;
  description: string;
  pubDate?: Date;
  updatedDate?: Date;
  heroImage?: any;
  tags?: string[];
  author?: string;
  readingTime?: string;
}

const { title, description, pubDate, updatedDate, heroImage, tags = [], author = 'Maples', readingTime } = Astro.props;
---

<html lang="en">
  <head>
    <BaseHead title={title} description={description} image={heroImage} />
  </head>
  <body>
    <Header />
    <main>
      <article class="entry-shell">
        <div class="entry-intro">
          <p class="eyebrow">Maples log</p>
          <h1>{title}</h1>
          <p class="description">{description}</p>
          <div class="meta-row">
            {(pubDate || updatedDate) && (
              <div class="dates">
                {pubDate && (
                  <span>
                    Published <FormattedDate date={pubDate} />
                  </span>
                )}
                {updatedDate && (
                  <span>
                    Updated <FormattedDate date={updatedDate} />
                  </span>
                )}
              </div>
            )}
            <p class="author-byline">By {author}{readingTime ? ` · ${readingTime}` : ''}</p>
          </div>
          {tags.length > 0 && (
            <ul class="tag-list">
              {tags.map((tag) => <li>{tag}</li>)}
            </ul>
          )}
        </div>

        {heroImage && (
          <div class="hero-image">
            <Image width={1400} src={heroImage} alt="" />
          </div>
        )}

        <div class="prose">
          <slot />
        </div>
      </article>
    </main>
    <Footer />
  </body>
</html>

<style>
  .entry-shell {
    display: grid;
    gap: 2rem;
  }
  .entry-intro,
  .prose {
    max-width: 760px;
    width: 100%;
  }
  .description {
    max-width: 44rem;
    font-size: 1.05rem;
    color: rgb(var(--text-secondary));
  }
  .meta-row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.8rem 1.25rem;
    margin-top: 1rem;
  }
  .dates {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    color: rgb(var(--text-secondary));
    font-size: 0.95rem;
  }
  .author-byline {
    margin: 0;
    color: rgb(var(--text-secondary));
    font-size: 0.95rem;
  }
  .hero-image img {
    width: 100%;
    box-shadow: var(--box-shadow);
  }
  .prose {
    padding: 2rem;
    border-radius: 1.6rem;
    background: rgba(var(--surface), 0.92);
    border: 1px solid rgba(var(--line), 0.16);
    box-shadow: var(--box-shadow);
  }
  :root[data-theme='dark'] .prose {
    background: rgba(var(--surface-muted), 0.9);
    border-color: rgba(var(--line), 0.3);
  }
  .prose :global(h2) {
    margin-top: 2rem;
  }
  @media (max-width: 720px) {
    .prose {
      padding: 1.25rem;
    }
  }
</style>
